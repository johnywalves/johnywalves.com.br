import styled from "styled-components"

export const Icon = styled.div`
  color: var(--highlight);
  height: 5rem;
  width: 5rem;
  margin: 0 auto 0;

  & svg {
    height: 100%;
    width: 100%;
  }
`
