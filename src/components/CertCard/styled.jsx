import styled from "styled-components"

export const Icon = styled.div`
  color: var(--highlight);
  height: 5rem;
  width: 5rem;
  transform: translateY(-25%);
  margin: 0 auto;

  & svg {
    height: 100%;
    width: 100%;
  }
`
