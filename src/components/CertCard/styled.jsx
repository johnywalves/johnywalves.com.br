import styled from "styled-components"

export const Icon = styled.div`
  color: var(--highlight);
  height: 6rem;
  width: 5rem;
  margin: 0 auto 0;
  padding-top: 1rem;

  & svg {
    height: 100%;
    width: 100%;
  }
`
