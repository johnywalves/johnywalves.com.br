import styled from "styled-components"

export const Wrapper = styled.ul`
    display: flex;
    flex-direction: row;
    justify-content: space-evenly;
    gap: 25px;
    list-style: none;
    flex-wrap: wrap;
`
